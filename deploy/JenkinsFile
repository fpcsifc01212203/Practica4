pipeline {
    agent any
    stages{
        stage ('listar directorio actual'){
            steps {
                sh 'ls -la'
            }
        }
        stage ('Build de docker'){
            steps{
                sh 'docker buils -t web:latest ./deploy/Dockerfile'
            }
        }
    }
}